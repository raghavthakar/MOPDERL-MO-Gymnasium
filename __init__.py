"""Top-level package for the MOPDERL project.

This file exists to make `mopderl` importable as a regular Python package so
modules can be executed reliably via:

    python -m mopderl.MOPDERL.run_mo_pderl ...
"""
